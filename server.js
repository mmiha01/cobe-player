// const path = require('path')
// const express = require('express')
// const app = express(),
//             DIST_DIR = __dirname,
//             MAIN_DIR = path.join(DIST_DIR, 'dist')
// app.use(express.static(MAIN_DIR))
// app.get('*', (req, res) => {
//     res.sendFile(MAIN_DIR)
// })
// const PORT = 3000
// app.listen(PORT)